# M2A 官方资料自动归档交接

日期：2026-09-20
分支：`codex/investment-research-m2a`
HEAD：`a6de59a53b96af11e2b569152de2a217905dfb29`（全部改动未提交）

## 已完成

1. 建立 `OfficialResearchDocument` 与 `ResearchSourceState`，含迁移、唯一约束、索引和来源状态。
2. 实现 SEC EDGAR 元数据客户端与幂等同步；严格限制官方主机、响应大小、重试与错误内容。
3. 实现 Microsoft IR 财报发现、正文纯文本提取和幂等同步。
4. 实现统一同步编排与 `sync_research_sources` 管理命令，支持证券、来源、数量与失败退出选项。
5. 实现档案内只读资料摘要、列表和详情页，保持现有 owner 权限边界。

## 用户看到的行为

- 打开微软研究档案即可看到官方资料数量与同步状态。
- 进入资料列表可查看 SEC 年报元数据和 Microsoft IR 财报正文，无需逐条手工录入链接、日期和摘要。
- 页面只读取已经归档的数据；抓取失败会保留旧资料，并在来源状态中显示短错误。

## 验证

- 离线全模块测试：**231/231 OK**。
- 系统检查、迁移一致性、diff whitespace 检查：全部通过。
- Microsoft IR 真实小流量开发机验收此前通过，首页发现、正文解析和重复同步幂等均正常。
- SEC 代码与 mock/边界测试通过，但开发机真实出口仍收到 403，需从 NAS 再验。
- M2A-5 桌面和窄屏浏览器验收通过，无横向溢出；GET 页面无同步、无写入。

## 尚未执行

- 未 commit、未 push、未部署。
- 未访问 NAS 或生产 PostgreSQL，生产数据零改动。
- 未配置 DSM 定时任务。
- 未实现 AI 摘要、自动把资料映射到关键假设/待验证问题、证据结论生成。

## 建议下一步

先做提交前范围审计并形成精确 commit。若用户决定上线，再按 NAS 部署流程备份数据库、核对关键表基线、部署该 commit、执行迁移，并从 NAS 出口分别验收 Microsoft IR 与 SEC；真实来源同步和 DSM 定时任务应在代码部署稳定后单独启用。
