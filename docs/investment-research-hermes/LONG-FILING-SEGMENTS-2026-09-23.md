# 长篇 SEC 正文分区段阅读（2026-09-23）

原先 AI 草稿固定发送正文开头 16,000 字；对数十万字的 10-K，后续内容没有可选入口。本次改为按不可变正文版本的字符位置划分不重叠区段，每段最多 16,000 字。成员在档案页明确选择“版本＋区段”、模型并逐次确认后，才发送所选区段；其余区段不随请求发送。区段只表示原文位置范围，不冒充 SEC 自身章节。

每个选项显示字符起止位置及是否已有成功草稿。草稿记录区段序号、总数和起止位置，页面明确指出其他区段本次未读；旧草稿继续按“开头部分”展示。AI 提示词限定摘要与判断只针对本次区段，引用仍由服务端绑定到不可变正文版本的全局字符位置和哈希。成功草稿只表示模型处理过所选区段，不代表成员已核实事实，也不会修改正式判断。

测试覆盖后续区段单独发送、前一区段不泄露、全局引用回到原文并高亮、非法区段在模型请求前拒绝、页面完成状态与所有相关模块回归。尚未用真实云端模型验收草稿质量；完整 10-K 需要成员逐段发起，当前没有自动全文综合报告。

## 生产部署与现状

提交 `a27489a3ac1725bec1447965486f0a499803179b` 已推送并部署到 NAS。部署前数据库备份为 `/volume1/docker/family-workbench/backups/family-workbench-pre-sec-segments-20260923-1116.dump`，SHA-256 `e83756bb7b6121d5134a85dae77faf8fda143d8b4de9905685e166bdb64ef118`，受限包装器验证了自包含格式。源码回滚包为 `/volume1/docker/family-workbench/backups/source-predeploy-422a0e753eda836077fa8feb883cb98882acfcf6-20260923-111520.tar.gz`，SHA-256 `626c7b8d11dbd1c65ad17a562e626d432757c4f684a37cdb4128e23b7ad61504`。精确提交归档 SHA-256 `c6b9ea0075166479bd730591880d7391d45872565a5bbe9e7e5a7a521c8472fb`，本地与 NAS 一致。

依赖与 Compose 未变，Web 已重启，启动日志显示无新迁移。生产 `check` 无问题，内部与外部投研入口均正常跳转登录；数据库与 OpenD 健康，`.env` 哈希不变。部署前后投资组合基线一致：InvestmentAccount 35、InvestmentPosition 474、InvestmentTransaction 1061、PortfolioSnapshot 2023、PortfolioSnapshotPositionLine 12534、DailyPortfolioValuationRun 69，最新快照日期 2026-09-23。NAS 运行标记已更新为上述提交。

首次生产浏览器检查时，微软档案可正常访问，但投研 AI 区域提示尚无获准处理投研资料的模型：现有 DeepSeek 文本模型只配置了其他用途，没有投研专用授权、费用与输入范围配置。因此当时没有调用真实模型。后续模型启用与一次草稿验收按成员逐次确认进行。

## 真实模型验收中的异常

成员随后明确授权：在现有 DeepSeek V4 Pro 上启用投研用途，单次估算费用上限 0.10 美元，并只发送微软这份 10-K 的第 10/22 区段及当前正式判断、关键假设和待验证问题一次。模型投研专用字段已添加，生产页面显示所选字符 144000–160000 与正确模型。首次提交立即返回 500；只读的 AI 请求列表没有新投研请求，说明失败发生在建立请求记录之前，未观察到云端发送。未重复点击。原生产日志没有给出应用层异常，因此补充只记录异常类型和代码位置、不记录正文与私人判断的诊断处理，待部署后定位与复试。
